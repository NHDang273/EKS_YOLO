apiVersion: v1
kind: ServiceAccount
metadata:
  name: yolo-sa
  namespace: yolo-inference
  annotations:
    # IAM role for S3 and EFS access (IRSA)
    # Will be replaced by deploy script with actual account ID
    eks.amazonaws.com/role-arn: arn:aws:iam::688567276212:role/yolo-eks-pod-role
